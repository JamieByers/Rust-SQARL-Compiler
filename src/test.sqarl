DECLARE condition INITIALLY 0
WHILE condition < 3 THEN
    SEND "outer" TO DISPLAY
    DECLARE inner INITIALLY 0
    WHILE inner < 3 THEN
        SEND "inner "+str(inner) TO DISPLAY
        SET inner TO inner+1
    END WHILE

    SET condition TO condition+1
END WHILE
